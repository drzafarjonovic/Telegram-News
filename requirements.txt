# Telegram bot framework (foydalanuvchilar bilan muloqot)
aiogram==3.13.1

# Telegram userbot (ochiq kanallarni o'qish uchun shaxsiy akkaunt)
telethon==1.36.0

# PostgreSQL (Supabase) asinxron drayver
asyncpg==0.29.0

# AI provayderlar (OpenAI-compatible: Groq, OpenAI, Gemini)
openai==1.54.4

# Vaqt jadvali (digest yuborish)
APScheduler==3.10.4

# Vaqt mintaqalari (Asia/Tashkent)
tzdata==2024.2

# Konfiguratsiya (.env fayldan o'qish)
python-dotenv==1.0.1

# ---------- Enterprise v2.0 (IXTIYORIY) ----------
# OCR (rasmdan matn) — faqat OCR_ENABLED=true bo'lsa kerak.
# Tizimda `tesseract-ocr` ham o'rnatilgan bo'lishi lozim (Dockerfile'da bor).
# pytesseract==0.3.13
# Pillow==10.4.0
#
# Speech-to-Text (Whisper) uchun alohida kutubxona shart emas —
# mavjud `openai` SDK ishlatiladi (STT_ENABLED=true + STT_API_KEY).
